{
  "name": "@dailyrecap/video",
  "version": "0.1.0",
  "private": true,
  "description": "Data-driven launch video: a script.json in, a 30-60s mp4 out. Rendered locally with Remotion, no video API needed.",
  "license": "MIT",
  "scripts": {
    "studio": "remotion studio src/index.ts",
    "render": "node scripts/render.mjs",
    "music": "node scripts/music.mjs",
    "narrate": "node scripts/narrate.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.500",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "remotion": "^4.0.500"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
