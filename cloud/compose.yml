services:
  agent:
    build:
      context: ..
      dockerfile: cloud/Dockerfile
    env_file: ../plow-credentials
    volumes:
      - state:/var/lib/plow
    stop_grace_period: 35s
volumes:
  state:
